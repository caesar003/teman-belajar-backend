const { answer } = require("./answer");
const { lesson } = require("./lesson");
const { student } = require("./student");
const { question } = require("./question");

module.exports = { answer, lesson, student, question };
